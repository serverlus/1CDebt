# DebtScoring — Инструкция по сборке и установке
## От исходников до работающего расширения 1С

---

## 🗂️ Структура проекта

```
debt_scoring_v2/
│
├── python/
│   └── scoring_engine.py          ← Python-движок (МОЗГ продукта)
│
├── addin_csharp/
│   ├── DebtScoring.csproj         ← Проект C# (.NET 4.8)
│   ├── DebtScoringAddIn.cs        ← Главный класс — мост 1С ↔ Python
│   ├── LicenseManager.cs          ← Управление лицензиями
│   ├── Resources/
│   │   └── scoring_engine.enc     ← (генерируется) зашифрованный Python
│   └── 1c_modules/
│       ├── ДС_СкоринговыйДвижок.bsl  ← Главный модуль 1С
│       └── ДС_Лицензия.bsl           ← Модуль лицензий 1С
│
├── build_tools/
│   ├── encrypt_python.py          ← Шифрует scoring_engine.py → .enc
│   └── pack_addin.py              ← Упаковывает DLL → zip для 1С
│
└── dist/                          ← (генерируется при сборке)
    └── DebtScoring.zip            ← Готовая внешняя компонента
```

---

## ⚙️ Что нужно установить на машине разработчика

```
1. .NET SDK 4.8 или .NET 6+
   https://dotnet.microsoft.com/download

2. Python 3.12 embedded (для вшивания в DLL)
   https://www.python.org/downloads/  → "embeddable package"
   Распакуй в: addin_csharp/python/

3. Visual Studio 2022 (или просто dotnet CLI)
   Community Edition — бесплатна
```

---

## 🔨 Сборка: пошаговая инструкция

### ШАГ 1 — Меняем соль шифрования

> ⚠️ ОБЯЗАТЕЛЬНО перед первым релизом!

Открой оба файла и замени одинаковую строку:

**`addin_csharp/DebtScoringAddIn.cs`**, строка ~30:
```csharp
private const string ENCRYPTION_SALT = "DS_SALT_REPLACE_IN_PRODUCTION_32C";
//                                       ↑↑↑ заменить на свою случайную строку
```

**`build_tools/encrypt_python.py`**, строка ~25:
```python
ENCRYPTION_SALT = "DS_SALT_REPLACE_IN_PRODUCTION_32C"
#                   ↑↑↑ та же самая строка!
```

Сгенерировать случайную соль:
```bash
python3 -c "import secrets; print(secrets.token_hex(16))"
```

---

### ШАГ 2 — Шифруем Python-код

```bash
cd debt_scoring_v2
python3 build_tools/encrypt_python.py
```

Ожидаемый вывод:
```
✓ Исходник: python/scoring_engine.py (18,432 байт)
✓ Ключ (SHA256 соли): a3f8c2d1...
✓ IV (случайный):    7b4e9a2c...
✓ Зашифровано: addin_csharp/Resources/scoring_engine.enc (18,448 байт)
✓ Верификация: расшифрованный код является валидным Python
✅ Готово!
```

---

### ШАГ 3 — Компилируем C# проект

```bash
cd debt_scoring_v2/addin_csharp
dotnet restore
dotnet build -c Release -r win-x64
```

Или через Visual Studio: File → Open → DebtScoring.csproj → Build → Release

После сборки в папке `bin/Release/net48/` появится:
- `DebtScoring.dll`    ← наша компонента
- `Python.Runtime.dll` ← pythonnet
- другие зависимости

---

### ШАГ 4 — Упаковываем в ZIP для 1С

```bash
cd debt_scoring_v2
python3 build_tools/pack_addin.py
```

Результат: `dist/DebtScoring.zip`

---

### ШАГ 5 — Создаём расширение 1С (.cfe)

1. Открой **Конфигуратор 1С**
2. **Конфигурация → Расширения конфигурации → Создать**
3. Имя расширения: `DebtScoring`, суффикс: `ДС`
4. Режим применения: `Основной`

**Добавляем внешнюю компоненту (макет):**
5. В дереве расширения: **Общие → Макеты → Добавить**
6. Имя макета: `ДС_КомпонентаСкоринга`
7. Тип макета: `Двоичные данные` (или `Внешняя компонента`)
8. Загрузить файл: `dist/DebtScoring.zip`

**Добавляем модули 1С:**
9. **Общие → Общие модули → Добавить**
   - Имя: `ДС_СкоринговыйДвижок`
   - Серверный: ✅, Без повторного использования: ✅
   - Вставь содержимое `1c_modules/ДС_СкоринговыйДвижок.bsl`

10. **Общие → Общие модули → Добавить**
    - Имя: `ДС_Лицензия`
    - Серверный: ✅
    - Вставь содержимое `1c_modules/ДС_Лицензия.bsl`

**Сохраняем расширение:**
11. **Конфигурация → Сохранить конфигурацию в файл → DebtScoring.cfe**

---

### ШАГ 6 — Установка у клиента

Клиент получает **один файл**: `DebtScoring.cfe`

Установка:
1. 1С → **Главное меню → Сервис → Расширения конфигурации**
2. Нажать **"+"** (Добавить)
3. Выбрать `DebtScoring.cfe`
4. Перезапустить 1С
5. Откроется **Мастер активации** → ввести лицензионный ключ

---

## 🔐 Система лицензирования

### Для тебя (генерация ключей):
```bash
# POST запрос к твоему серверу
curl -X POST https://license.твойдомен.ru/api/v1/generate-keys \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -d '{"plan": "PRO", "count": 10, "months": 12}'
```

### Форматы ключей:
```
DSCR-A1B2-C3D4-E5F6-G7H8   ← стандартный (1 год, 1 пользователь)
DSCR-PRO-XXXX-XXXX-XXXX    ← Pro тариф
DSCR-CORP-XXXX-XXXX-XXXX   ← Corporate тариф
```

### Привязка лицензии:
- ИНН организации + MAC-адрес сервера 1С
- Хранится в реестре Windows (шифрование DPAPI)
- Онлайн-валидация раз в 24 часа
- Grace-период без интернета: 72 часа

---

## 🧪 Проверка работоспособности в 1С

После установки и активации в консоли запросов 1С:

```bsl
// Тест 1: Проверка лицензии
Результат = ДС_Лицензия.ЛицензияДействительна();
Сообщить("Лицензия: " + Результат);

// Тест 2: Скоринг конкретного контрагента
Контрагент = Справочники.Контрагенты.НайтиПоНаименованию("ООО Ромашка");
Скоринг    = ДС_СкоринговыйДвижок.РассчитатьСкоринг(Контрагент);
Сообщить("Скоринг: " + Скоринг.Скоринг + " / " + Скоринг.МеткаРиска);
Сообщить("Лимит:   " + Скоринг.РекомендуемыйЛимит + " ₽");

// Тест 3: Пакетный анализ всей дебиторки
ТаблицаРисков = ДС_СкоринговыйДвижок.АнализВсейДебиторки();
Сообщить("Контрагентов с дебиторкой: " + ТаблицаРисков.Количество());
```

---

## ❓ Частые вопросы

**Q: Нужен ли Python на машине клиента?**
A: Нет! Python 3.12 embedded вшит внутрь DLL. Клиент ничего не устанавливает.

**Q: Можно ли скопировать DLL и использовать без ключа?**
A: Нет. Python-код зашифрован AES-256, ключ шифрования зависит от лицензионного токена, который привязан к ИНН + MAC-адресу.

**Q: Работает ли без интернета?**
A: Да, 72 часа. После — требует онлайн-валидацию.

**Q: Какие конфигурации 1С поддерживаются?**
A: Любые на платформе 8.3.21+. Запросы в `ДС_СкоринговыйДвижок.bsl` адаптируются под конкретную конфигурацию.

**Q: Как обновить Python-движок не меняя расширение?**
A: На данный момент — пересобрать и выпустить новую версию .cfe. В следующей версии планируется hot-reload через CDN.
